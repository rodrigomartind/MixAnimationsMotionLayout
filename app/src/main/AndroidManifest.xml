<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rodrigodominguez.mixanimationsmotionlayout">

    <application
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".circularcards.CircularCardsScene1Activity"></activity>
        <activity android:name=".circularcards.CircularCardsHomeActivity" />
        <activity android:name=".rotationcard.RotationCardHomeActivity" />
        <activity android:name=".storiesinstagram.InstagramStoryActivity" />
        <activity android:name=".rotationcard.RotationCardActivity"/>
        <activity android:name=".verticalpilcards.VerticalStackCardsActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>